package notstore

// notstore is not a repository package, so this SQL-looking prose must NOT be
// flagged. This guards against false positives outside the repository layer.
const menu = "select your meal from the menu where you are seated"
